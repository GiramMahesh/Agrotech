import 'package:flutter/material.dart';

class TermsAndConditions extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Terms and Contions'),
        backgroundColor: Colors.green,
      ),

      body: Center(
        child: Text('This is Terms and Conditions page. Development is under process'),
      ),
    );
  }
}
